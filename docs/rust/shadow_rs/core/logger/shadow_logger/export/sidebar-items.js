initSidebarItems({"fn":[["shadow_logger_init","Creates a ShadowLogger and installs it as the default logger for Rust’s `log` crate. The returned pointer is never deallocated, since loggers registered with the `log` crate are required to live for the life of the program."],["shadow_logger_setEnableBuffering","When disabled, the logger thread is notified to write each record as soon as it’s created.  The calling thread still isn’t blocked on the record actually being written, though."]]});